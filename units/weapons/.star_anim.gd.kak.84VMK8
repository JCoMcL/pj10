extends Sprite2D

const framerate = 60
const frametime = 1.0 / framerate
var accum = 0.0
func _process(delta: float) -> void:
	accum += delta
	while accum > frametime:
		accum -= frametime
		frame = [0,1,2,3].pick_random()


